version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  secondary-artifacts:
    website:
      files:
        - 'src/web/*'
    api:
      files:
        - 'src/functions/*.ts'