{
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@broadlify/config": "*",
    "@types/aws-lambda": "^8.10.97",
    "@types/node": "17.0.35",
    "esbuild": "^0.14.39"
  },
  "name": "@broadlify/api",
  "scripts": {
    "build": "esbuild src/greeting.ts --bundle --format=cjs --platform=node --target=node14",
    "dev": "esbuild src/* --bundle --format cjs --platform node --target node14 --sourcemap --outdir dist --watch",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src"
  },
  "version": "0.0.0"
}