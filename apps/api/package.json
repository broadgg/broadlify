{
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@broadlify/config": "*",
    "@types/aws-lambda": "^8.10.97",
    "@types/node": "17.0.35",
    "esbuild": "^0.14.39",
    "ts-node": "^10.7.0",
    "nodemon": "^2.0.16"
  },
  "name": "@broadlify/api",
  "scripts": {
    "build": "tsc && ts-node scripts/build.ts",
    "dev": "nodemon src/* --exec tsc",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src"
  },
  "version": "0.0.0"
}